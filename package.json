{
  "name": "biasguard-ai-platform",
  "version": "1.0.0",
  "description": "BiasGuard AI — AI Bias Detection & Fairness Audit Platform",
  "main": "backend/src/server.js",
  "scripts": {
    "start": "node backend/src/server.js",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "render-build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "dev:backend": "node backend/src/server.js",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "keywords": [
    "ai-fairness",
    "bias-detection",
    "responsible-ai",
    "bias-bounty"
  ],
  "author": "BiasGuard AI Team",
  "license": "MIT"
}
